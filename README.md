# BATDiff: Bivariate A Trous Wavelet Diffusion

This repository provides the implementation of BATDiff for single image super-resolution.

## Overview

BATDiff introduces a multiscale diffusion framework using an à trous wavelet decomposition with bivariate cross-scale conditioning.

## Installation

```bash
pip install -r requirements.txt
